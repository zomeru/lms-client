import type { NextPage } from 'next';

import { Layout, Navbar } from '@/components';

const Home: NextPage = () => {
  return (
    <Layout>
      <Navbar></Navbar>
    </Layout>
  );
};

export default Home;
