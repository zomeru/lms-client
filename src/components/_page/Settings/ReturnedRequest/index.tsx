import React from 'react';

import RequestItems from '../RequestItems';

const ReturnedRequest = () => {
  return <RequestItems status="returned" header="Returned books" />;
};

export default ReturnedRequest;
