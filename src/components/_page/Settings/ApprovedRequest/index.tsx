import React from 'react';

import RequestItems from '../RequestItems';

const ApprovedRequest = () => {
  return <RequestItems status="approved" header="Approved requests" />;
};

export default ApprovedRequest;
